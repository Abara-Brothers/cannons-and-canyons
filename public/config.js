// Runtime configuration for Canyons & Cannons.
//
// CC_SERVER: leave null when the game is served by its own Node server (web).
// For PACKAGED app-store builds (Capacitor/WebView), the page is loaded from
// the app bundle, so point this at your deployed game server host, e.g.:
//   window.CC_SERVER = 'canyons-and-cannons.onrender.com';
window.CC_SERVER = null;
